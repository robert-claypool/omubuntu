# omubuntu tmux configuration (user stub)
# omubuntu-managed defaults live at: ~/.config/omubuntu/tmux.conf
# This file is safe to edit; managed defaults may be updated automatically.

if-shell 'test -f ~/.config/omubuntu/tmux.conf' 'source-file ~/.config/omubuntu/tmux.conf'